# Open-PR Triage — graphwork/wg (2026-06-24)

**Scope:** ANALYSIS ONLY. Nothing in this task is merged, closed, rebased, or
pushed to any PR. This document is a disposition report + an enumerated list of
follow-up resolution tasks for the chat agent to dispatch.

**Baseline:** all mergeability below is a **real trial-merge against current
`main`** (`4a1e4a7d` — `feat: fix-pi-handler`), computed in-memory with
`git merge-tree --write-tree main pr/<n>` (does not touch the shared worktree).
The GitHub `mergeable`/`mergeStateStatus` fields happened to be current this time
(not the usual `UNKNOWN`), and the trial-merge agreed with them — but the
trial-merge additionally yields the **exact conflicting-file list** per PR, which
drives the ordering below.

**CI:** only #42 and #38 (the two `ekg` PRs) have CI runs recorded (both green,
but stale). Every `njt` and `lucapinello` PR shows *"no checks reported"* — CI
never ran on those branches (outside-contributor approval gate / branches predate
the workflow). Treat their CI as **unknown** until rebased + re-run.

**Inventory:** 19 open PRs. `#26`/`#35` resolve to non-PR numbers; `#39–#41` are
not open. All 19 open PRs are dispositioned below — none skipped.

---

## Disposition table (one row per open PR)

| # | Title | Author | Size | Real mergeability (trial-merge vs `main`) | CI | Disposition | Rationale |
|---|-------|--------|------|-------------------------------------------|----|-------------|-----------|
| **42** | keep executor and model routing atomic | ekg | +249/-21 | **CONFLICT** — `coordinator.rs`, `dispatch/plan.rs` | pass (stale) | **subsumed-by-main (close)** | This week's handler-first work made the handler *always* derived from the model spec (`handler_for_model` = single source of truth), so the (executor, model) mismatch #42's `validate_cli_backend_match` guards is now structurally impossible. Main already ships the exact smoke scenario #42 adds (`task_agent_codex_model_routes_to_codex.sh`) **plus** `native_floor_yields_to_cli_locked_model.sh`, whose description explicitly states it "closes the residual gap where `validate_cli_backend_match` only guarded the claude/codex executors." Fully superseded. |
| **38** | migrate rquest → git-pinned v3.0.6 + rquest-util | ekg | +680/-663 | **CONFLICT** — `Cargo.toml` + 12 more (`chat_cmd.rs`, `reprioritize.rs`, `coordinator_agent.rs`, `coordinator.rs`, 8 test files) | pass (stale) | **subsumed-by-main → needs-author then close** | Main **already migrated to rquest 3.x** (`rquest = "3.0.0"` from crates.io, comment: *"built-in Chrome impersonation, so no rquest-util shim is needed"*). #38's distinct approach (git-pinned `v3.0.6` + sibling `rquest-util` + transitive-dep override) is now obsolete unless v3.0.6 fixes something 3.0.0 lacks. Base = **May 14**, massive conflicts incl. the whole test suite. Confirm with author there's no v3.0.6-only need, then close. |
| **37** | multi-bot Telegram (one bot per named agent) | lucapinello | +436/-22 | **CLEAN** | none | **merge-now** | Clean trial-merge; genuine new feature. File-collides with #31 (`cli.rs`,`main.rs`) and #21 (`telegram.rs`) → serialize after #31. |
| **36** | probe for gtimeout/timeout at wrapper runtime | lucapinello | +97/-11 | **CLEAN** | none | **merge-now — via existing `resolve-merge-pr-36`** | Clean. **Overlaps #22** (same timeout-portability problem, different code path — see overlaps §). Already has a dedicated resolution task — reference it, do **not** duplicate. |
| **34** | resolve Git-for-Windows bash, skip WSL shim | njt | +477/-22 | **CONFLICT** — `config.rs` (~27 lines) only | none | **resolve-conflicts (small)** | Single-file conflict; everything else merges clean. Touches `execution.rs`/`worktree.rs`/`background.rs` → collides with the `\\?\` group and the deep port. |
| **33** | keep state.json when daemon alive but socket slow | njt | +629/-315 | **CONFLICT** — `Cargo.lock`, `key.rs`, `coordinator_agent.rs`, `ipc.rs`, `service/mod.rs`, `spawn/worktree.rs`, `background.rs`, `screen_dump.rs` | none | **resolve-conflicts (HEAVY) — the real deep Windows port** | `#33 = #19's 7 commits + 1 extra` (`3d793cd8` state.json fix); same file set. This is the structural cross-platform port (local sockets, ACLs, junctions, clean interrupts, Unix-API gating). Base = **Apr 18**; since then main landed **26 commits on `service/mod.rs`** and **40 on `ipc.rs`** → major divergence. Highest-risk item; needs a careful dedicated rebase. Merge **this**, not #19. |
| **32** | CREATE_NEW_PROCESS_GROUP on inline eval+assign | njt | +28/-0 | **CLEAN** | none | **merge-now** | Tiny, single file (`coordinator.rs`). Complementary to #29 (process-group pair, different files). |
| **31** | `wg doctor` environment diagnostic | njt | +534/-0 | **CLEAN** | none | **merge-now** | Mostly a new file (`doctor.rs`); genuinely-new top-level `wg doctor`. (Main's *"Doctor:"* doc-comment at `cli.rs:4427` is on the existing `Check` subcommand — **no** name collision.) Collides `cli.rs`/`main.rs` with #37 → serialize before #37. |
| **30** | strip CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST etc. | njt | +34/-5 | **CONFLICT** — `coordinator_agent.rs` (~10 lines) | none | **resolve-conflicts (small)** | Small conflict. Touches `llm.rs` (collides #22/#23/#27) and `coordinator_agent.rs` (collides #27 + deep port) → serialize with #27. |
| **29** | CREATE_NEW_PROCESS_GROUP on Windows task-agent spawn | njt | +12/-0 | **CLEAN** | none | **merge-now (land first in the `execution.rs` cluster)** | Tiny, single file (`execution.rs`). Land before #24/#28/#30/#34/#27/#36 so they rebase onto it once. |
| **28** | strip `\\?\` on prompt + output-log paths in wrapper | njt | +42/-7 | **CONFLICT** — `execution.rs` | none | **resolve-conflicts + CONSOLIDATE with #24** | Uses `super::sanitize_bash_path`; **#24 adds a competing `strip_verbatim_prefix` helper in the same file** → #24 and #28 conflict with each other. Consolidate the `\\?\` group (#24/#25/#28) onto one helper. |
| **27** | home the Claude Code OAuth token + Windows README | njt | +334/-3 | **CONFLICT** — `coordinator_agent.rs`, `service/llm.rs` | none | **resolve-conflicts (medium)** | Largest of the medium group; `README-windows.md` (+198) is new (no conflict). Touches `config.rs` → verify additive vs this week's config work. Serialize after #30 (shared `coordinator_agent.rs`+`llm.rs`). |
| **25** | strip `\\?\` prefix on paths handed to `git worktree` | njt | +76/-12 | **CLEAN** | none | **merge-now (align with `\\?\` group)** | Clean; only `worktree.rs` (file-independent of #24/#28, same conceptual group). Collides `worktree.rs` with #34 + deep port. |
| **24** | strip `\\?\` prefix before handing path to bash | njt | +37/-3 | **CONFLICT** — `execution.rs` | none | **resolve-conflicts + CONSOLIDATE with #28** | Competing helper vs #28 (see #28). Pick one shared helper for the whole `\\?\` group. |
| **23** | log claude CLI stdout alongside stderr on failure | njt | +4/-2 | **CLEAN** | none | **merge-now (trivial)** | Diagnostic-only, single hunk in `llm.rs`. Not present on main. Land first. |
| **22** | cross-platform replacement for `timeout(1)` | njt | +156/-34 | **CONFLICT** — `lib.rs` (the `pub mod` line only) | none | **rebase-only (trivial) + flag #36 overlap** | New `platform_timeout.rs` + `llm.rs`/`tools` edits all merge clean; **only** the `lib.rs` module-list line conflicts. **Overlaps #36** (both solve `timeout(1)` portability; different code paths — see overlaps §). |
| **21** | use `dirs::home_dir()` instead of `$HOME` | njt | +14/-22 | **CONFLICT** — `setup.rs` | none | **resolve-conflicts (small)** | Still relevant — main still uses `$HOME`/`env::var("HOME")` in `setup.rs` (×6), `service/mod.rs`, `skills.rs`, `telegram.rs`. Small conflict. Shares `service/mod.rs` with #20 + deep port → serialize. |
| **20** | stop heartbeat spin when `--no-coordinator-agent` | njt | +23/-2 | **CONFLICT** — `service/mod.rs` | none | **resolve-conflicts (small)** | Small logical gate; conflicts only because `run_daemon` shifted. Still relevant. Shares `service/mod.rs` with #21 + deep port. |
| **19** | Windows support for the full daemon and CLI (umbrella) | njt | +603/-308 | **CONFLICT** — same 8-file set as #33 | none | **superseded-by-#33 (close)** | `#33` is `#19` + one extra commit with an identical file set (diff between the two branches is only +26/-7 on `service/mod.rs`). Merging #33 lands everything #19 has. Close #19; do the work via #33. |

### Disposition summary
- **merge-now (clean):** #23, #25, #29, #31, #32, #37 — plus #36 via its existing task.
- **rebase-only / trivial conflict:** #22.
- **resolve-conflicts (small):** #20, #21, #30, #34.
- **resolve-conflicts (medium):** #27.
- **resolve-conflicts + consolidate:** #24 + #28 (the `\\?\` pair).
- **resolve-conflicts (heavy):** #33 (deep Windows port).
- **close — superseded:** #19 (by #33).
- **close — subsumed-by-main:** #42 (handler-first), #38 (rquest 3.0.0; confirm with author first).

---

## Overlap / supersession pairs (all flagged)

1. **#19 ⊂ #33 (supersession).** #33 = #19's exact 7 commits + 1 (`3d793cd8`); identical file set. → **Close #19**, merge #33.
2. **#36 ↔ #22 (timeout portability — same problem, different approach).**
   - #36 (lucapinello): keep the shell `timeout`, **probe for `gtimeout`/`timeout`** at wrapper runtime and substitute via `$WG_TIMEOUT_BIN`. Touches the **spawn wrapper** (`spawn/execution.rs`, `spawn/mod.rs`).
   - #22 (njt): **pure-Rust replacement** for `timeout(1)` (`platform_timeout.rs`). Touches **in-process call sites** (`service/llm.rs`, `tools/map.rs`, `tools/reader.rs`, `lib.rs`).
   - Different files → not a hard conflict, but the same root cause (macOS/Windows have no GNU `timeout`). **Decide on one strategy.** Main currently still hard-codes bare `timeout` in the wrapper *and* has no `platform_timeout.rs`, so the problem is unsolved on main and **both** angles are still relevant. Recommend: land #36 for the wrapper (its existing task), and scope #22 to the non-wrapper call sites so they don't re-implement each other.
3. **#24 ↔ #28 (competing `\\?\`-stripping helpers in the SAME file).** #24 adds local `strip_verbatim_prefix` in `execution.rs`; #28 uses `super::sanitize_bash_path` in `execution.rs`+`mod.rs`. They conflict with each other. **#25** is the same conceptual `\\?\` group but in a different file (`worktree.rs`). → **Consolidate #24+#25+#28 onto one shared helper.**
4. **#29 ↔ #32 (CREATE_NEW_PROCESS_GROUP — complementary, NOT competing).** #29 = task-agent spawn (`execution.rs`); #32 = inline eval+assign spawns (`coordinator.rs`). Different files, both clean → land both.
5. **#30 / #27 (shared files).** Both touch `coordinator_agent.rs` + `service/llm.rs` → serialize (#30 then #27).
6. **#42 → subsumed by main's handler-first work** (see table).
7. **#38 → subsumed by main's rquest 3.0.0 migration** (see table).
8. **Deep-port file overlaps (#19/#33 vs small fixes).** #33 shares files with several small PRs and so cannot land independently of them:
   - `service/mod.rs` ← #20, #21
   - `spawn/worktree.rs` ← #25, #34
   - `coordinator_agent.rs` ← #27, #30
   - `executor/native/background.rs` ← #34

### File-collision hotspots (drives serialization)
| File | Open PRs touching it |
|------|----------------------|
| `src/commands/spawn/execution.rs` | #24, #27, #28, #29, #30, #34, #36 (7) |
| `src/service/llm.rs` | #22, #23, #27, #30 |
| `src/commands/service/coordinator_agent.rs` | #19, #27, #30, #33, #38 |
| `src/commands/service/coordinator.rs` | #32, #34, #38, #42 |
| `src/commands/service/mod.rs` | #19, #20, #21, #33 |
| `src/commands/spawn/worktree.rs` | #19, #25, #33, #34 |
| `src/config.rs` | #27, #34, #38, #42 |
| `src/lib.rs` | #22, #34, #38 |
| `src/cli.rs`, `src/main.rs` | #31, #37 |
| `Cargo.lock` / `Cargo.toml` | #19, #33, #38 |

---

## njt Windows stack — structure & recommended sequence

**Structure (not what the umbrella framing implies):**
- **#19 / #33 are the only "deep" port** — 7 commits off base `40e68d93` (Apr 18) doing cross-platform sockets / ACLs / junctions / interrupts / Unix-API gating. #33 adds an 8th commit. They touch `Cargo.*` + the daemon socket internals.
- **#20–#34 are INDEPENDENT single-commit fixes**, each branched off `70d076db` (or later) — **they are NOT stacked on #19.** Each one rebases directly onto current `main` on its own. #19 is *not* a prerequisite base for them.

So the stack is **one heavy structural PR (#33, superseding #19) + ~12 independent leaf fixes that happen to share some files with it.** The only ordering constraint is the file-collision serialization above, not a dependency chain.

**Recommended Windows-stack sequence:**
1. **Clean leaves first** (no rebase needed today): #29 (land first in the `execution.rs` cluster), #32, #25, #31, #23.
2. **Trivial/small leaf conflicts:** #22 (lib.rs line; coordinate w/ #36), #20, #21 (serialize the two `service/mod.rs` touchers), #34.
3. **`\\?\` consolidation:** #24 + #28 (one helper), aligned with #25 — after #29.
4. **`coordinator_agent.rs`/`llm.rs` chain:** #30 then #27.
5. **Deep port LAST:** close #19; rebase + merge #33 against the post-leaf `main` in one focused heavy pass (it must be fully re-resolved over 2 months of `ipc.rs`/`mod.rs` churn regardless). Doing it last means its single rebase absorbs the leaf changes, instead of blocking 12 small wins behind it.

(`#36` lands in parallel via its own existing task; `#37` is lucapinello, not part of the njt stack — order it after #31 due to `cli.rs`/`main.rs`.)

---

## Whole-backlog prioritized merge order (quick clean wins → stacked/conflicting → hard)

**Tier 0 — close, no merge work:**
- **Close #42** (subsumed-by-main: handler-first).
- **Close #19** (superseded by #33).
- **#38** — confirm with author there's no v3.0.6-only need, then **close** (subsumed by main's rquest 3.0.0).

**Tier 1 — clean quick wins (merge in this order to touch each hotspot once):**
1. **#23** (trivial `llm.rs`)
2. **#29** (`execution.rs`, tiny — land before the rest of the `execution.rs` cluster)
3. **#32** (`coordinator.rs`, tiny)
4. **#25** (`worktree.rs`)
5. **#31** (`wg doctor`) — before #37
6. **#37** (multi-bot Telegram) — after #31
7. **#36** (timeout wrapper probe) — via existing `resolve-merge-pr-36`

**Tier 2 — small conflicts, easy rebase (after Tier 1):**
8. **#22** (only the `lib.rs` `pub mod` line; coordinate scope with #36)
9. **#20** then **#21** (both `service/mod.rs`)
10. **#24 + #28 consolidated** (`\\?\` helper) — after #29
11. **#30** (`coordinator_agent.rs`/`llm.rs`) — after #23
12. **#34** (`config.rs` + `execution.rs`/`worktree.rs`/`background.rs`) — after #29/#25

**Tier 3 — medium conflict:**
13. **#27** (OAuth token homing) — after #30 (shared files)

**Tier 4 — heavy, last:**
14. **#33** (deep Windows port; supersedes #19) — dedicated heavy rebase over post-Tier-1/2/3 `main`.

---

## Enumerated follow-up RESOLUTION TASKS to create (do NOT create here)

> One per actionable PR or coherent group. Suggested ids + dependencies for the
> chat agent to dispatch. `#36` is intentionally **omitted** — it already has
> `resolve-merge-pr-36`.

1. **`close-pr-42-subsumed`** — Close #42 with a comment pointing at main's handler-first work + `native_floor_yields_to_cli_locked_model.sh`. *(No code; cleanup.)*
2. **`close-pr-19-superseded`** — Close #19 noting #33 supersedes it (= #19 + 1 commit). *(No code; cleanup. Couple to task 13.)*
3. **`triage-pr-38-rquest`** — Ask author whether git-pinned rquest `v3.0.6`+`rquest-util` offers anything over main's crates.io `3.0.0`; if not, close as subsumed. *(needs-author.)*
4. **`merge-clean-pr-23`** — Rebase + merge #23 (trivial stdout-logging). Quick win; land first.
5. **`merge-clean-pr-29`** — Rebase + merge #29 (task-agent process group). Land before the `execution.rs` cluster.
6. **`merge-clean-pr-32`** — Rebase + merge #32 (inline eval+assign process group).
7. **`merge-clean-pr-25`** — Rebase + merge #25 (`\\?\` for `git worktree`); align helper with task 11.
8. **`merge-clean-pr-31`** — Rebase + merge #31 (`wg doctor`). Land before task 9.
9. **`merge-pr-37-telegram`** — Rebase + merge #37 (multi-bot Telegram). `--after` task 8 (`cli.rs`/`main.rs`).
10. **`resolve-pr-22-timeout`** — Rebase #22 (only `lib.rs` line conflicts); **coordinate scope with #36/`resolve-merge-pr-36`** so wrapper vs in-process timeout aren't double-solved.
11. **`consolidate-windows-verbatim-paths`** — Resolve #24 + #28 onto **one** shared `\\?\`-stripping helper (reconcile `strip_verbatim_prefix` vs `sanitize_bash_path`); keep #25 aligned. `--after` task 5.
12. **`resolve-pr-20-21-home-heartbeat`** — Resolve #20 then #21 (both `service/mod.rs`); small. Can be one task (serialized) or two with #20 `--before` #21.
13. **`resolve-pr-30-then-27`** — Resolve #30, then #27 (shared `coordinator_agent.rs`+`llm.rs`); #27 `--after` #30. (#27 also touches `config.rs` — verify additive vs this week's config work.)
14. **`resolve-pr-34-windows-bash`** — Resolve #34 (`config.rs` conflict; Git-for-Windows bash / WSL-shim skip). `--after` tasks 5 & 11 (shared `worktree.rs`/`execution.rs`).
15. **`resolve-pr-33-deep-windows-port`** — **Heavy.** Rebase + re-resolve #33 (cross-platform sockets/ACLs/junctions + state.json) over current `main` (26 `service/mod.rs` + 40 `ipc.rs` commits of churn). Do **last**, `--after` the Tier-1/2/3 Windows tasks; closes #19 on merge (couple to task 2).

**Suggested grouping for dispatch:** Tasks 4–9 are independent clean wins (parallel-safe except the `cli.rs`/`main.rs` pair 8→9 and the `execution.rs` order 5-before-11). Tasks 10–14 are the conflict-resolution mid-tier. Task 15 is the single heavy effort and should not block the others.
