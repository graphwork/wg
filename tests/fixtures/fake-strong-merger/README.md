# Fake strong merger

Credential-free adapter for the strong merge-resolution lane. It rejects leaked provider credentials, non-handler-first/unbound route inputs, unsupported reasoning, missing capability/sandbox CIDs, shared graph access, and retained Git remotes. It emits the strict `resolved` schema and deterministically writes `value.txt` in the isolated integration repository.
